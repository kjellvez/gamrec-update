# gamrec-update
Automate Google Workspace recovery email field update with GAM7. Quickly parse secondary emails locally (avoiding API limits) to fill missing recovery fields. Includes Windows (PowerShell) and Linux (Bash) versions with a safe Dry Run toggle.
